/**
 * All'interno di soundsHub troviamo tutti gli oggetti audio del gioco, per poterli rendere vidibili in ogni parte del codie
 * si e' reso neccessario creare una variabile globale
 * @type {internal}
 */

const soundsHub = {};

module.exports = soundsHub;